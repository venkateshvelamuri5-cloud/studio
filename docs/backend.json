
{
  "entities": {
    "User": {
      "title": "User",
      "description": "A student rider or administrative user profile.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "fullName": { "type": "string" },
        "phoneNumber": { "type": "string" },
        "collegeName": { "type": "string" },
        "studentId": { "type": "string" },
        "city": { "type": "string", "enum": ["Vizag", "Vizianagaram"] },
        "role": { "type": "string", "enum": ["rider", "admin", "driver"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "fullName", "phoneNumber", "role"]
    }
  },
  "auth": {
    "providers": ["phone"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "User profile document identified by Firebase Auth UID."
    }
  }
}
